{
    "name": "ottbrowser-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "services/*",
        "packages/*"
    ],
    "scripts": {
        "tv": "npm run start --workspace=apps/tv",
        "worker:deploy": "npm run deploy --workspace=services/edge-worker"
    }
}