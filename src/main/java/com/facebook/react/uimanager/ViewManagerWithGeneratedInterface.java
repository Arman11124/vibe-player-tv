package com.facebook.react.uimanager;

/**
 * Stub interface to satisfy libraries expecting codegen interfaces when using react-native-tvos.
 * It mirrors the name used by new architecture builds but carries no methods for old-arch usage.
 */
public interface ViewManagerWithGeneratedInterface<T> {
}

